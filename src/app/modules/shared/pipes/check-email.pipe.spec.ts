import { CheckEmailPipe } from './check-email.pipe';

describe('CheckEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
